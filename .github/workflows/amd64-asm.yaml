name: amd64-asm

on:
  push:
    branches:
      - "codex/amd64-avo-asm"
      - "codex/amd64-avo-asm-v2"
  pull_request:
    branches:
      - "main"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "stable"
          check-latest: true
          cache-dependency-path: |
            go.mod
            asm/go.sum
            benchmark/go.mod
      - name: Golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
      - name: Verify root module and source
        run: |
          set -euo pipefail
          go build ./...
          gofmt -d .
          test -z "$(gofmt -l .)"
          go vet ./...
          go fix -diff ./...
          go mod tidy -diff
          make check-main-deps vartime-audit
      - name: Test default backend
        run: go test -v -race -count=1 -cover ./...
      - name: Fuzz default sign, verify, and recovery path
        run: go test -run '^$' -fuzz=FuzzSignVerifyRecoverDigest -fuzztime=10s .
      - name: Verify benchmark module
        working-directory: ./benchmark
        run: |
          set -euo pipefail
          go build ./...
          gofmt -d .
          test -z "$(gofmt -l .)"
          go vet ./...
          go fix -diff ./...
          go mod tidy -diff
          go test -race -count=1 .

  correctness:
    strategy:
      matrix:
        goamd64:
          - v1
          - v3
    runs-on: ubuntu-24.04
    env:
      GOAMD64: ${{ matrix.goamd64 }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "stable"
          check-latest: true
          cache-dependency-path: |
            go.mod
            asm/go.sum
      - name: Verify generator module
        run: make check-asm-module
      - name: Verify generated files
        run: make generate-check
      - name: Build and vet tagged AMD64 backend
        run: |
          go build -tags=secp256k1_asm ./...
          go vet -tags=secp256k1_asm ./...
      - name: Test tagged AMD64 backend
        run: go test -v -race -count=1 -tags=secp256k1_asm ./...
      - name: Fuzz tagged base-field arithmetic
        run: go test -tags=secp256k1_asm -run '^$' -fuzz=FuzzElementArithmetic -fuzztime=10s ./internal/field
      - name: Fuzz tagged scalar arithmetic
        run: go test -tags=secp256k1_asm -run '^$' -fuzz=FuzzScalarMontgomeryBackend -fuzztime=10s ./internal/scalar
      - name: Constant-time smoke
        env:
          SECP256K1_CT_TEST: "1"
        run: go test -count=1 -tags=secp256k1_asm -run TestConstantTimeScalarBaseMultSmoke .
      - name: Disassemble and audit kernels
        run: ./scripts/check-amd64-asm.sh "${RUNNER_TEMP}/amd64-audit"
      - name: Upload disassembly
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: amd64-${{ matrix.goamd64 }}-disassembly
          path: ${{ runner.temp }}/amd64-audit

  native-windows:
    strategy:
      matrix:
        goamd64:
          - v1
          - v3
    runs-on: windows-2025-vs2026
    env:
      GOAMD64: ${{ matrix.goamd64 }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "stable"
          check-latest: true
      - run: go build -tags=secp256k1_asm ./...
      - run: go vet -tags=secp256k1_asm ./...
      - run: go test -race -count=1 -tags=secp256k1_asm ./...

  cross-build-darwin:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "stable"
          check-latest: true
      - name: Cross-build Darwin AMD64 v1
        env:
          GOOS: darwin
          GOARCH: amd64
          GOAMD64: v1
        run: go build -tags=secp256k1_asm ./...
      - name: Cross-build Darwin AMD64 v3
        env:
          GOOS: darwin
          GOARCH: amd64
          GOAMD64: v3
        run: go build -tags=secp256k1_asm ./...

  performance:
    strategy:
      fail-fast: false
      matrix:
        goamd64:
          - v1
          - v3
    runs-on: ubuntu-24.04
    env:
      GOAMD64: ${{ matrix.goamd64 }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: "stable"
          check-latest: true
      - name: Record runner and feature state
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/amd64-bench"
          lscpu >"${RUNNER_TEMP}/amd64-bench/lscpu.txt"
          go env >"${RUNNER_TEMP}/amd64-bench/go-env.txt"
          go test -v -count=1 -tags=secp256k1_asm -run TestAMD64FeatureDispatch ./internal/field \
            | tee "${RUNNER_TEMP}/amd64-bench/cpuid.txt"
          go test -v -count=1 -tags=secp256k1_asm -run TestAMD64ScalarFeatureDispatch ./internal/scalar \
            | tee -a "${RUNNER_TEMP}/amd64-bench/cpuid.txt"
          grep -q 'CPUID ADX+BMI2=true' "${RUNNER_TEMP}/amd64-bench/cpuid.txt"
          for kernel in none mul square; do
            SECP256K1_AMD64_BENCH_KERNEL="${kernel}" \
              go test -count=1 -tags='secp256k1_asm,secp256k1_amd64_bench' \
                -run '^TestAMD64BenchmarkKernelSelection$' ./internal/field
          done
          for kernel in accepted scalar-mul scalar-square scalar-squaren scalar invvartime 'scalar,invvartime' all; do
            SECP256K1_AMD64_V2_BENCH_KERNEL="${kernel}" \
              go test -count=1 -tags='secp256k1_asm,secp256k1_amd64_bench' \
                -run '^TestAMD64V2BenchmarkKernelSelection$' ./internal/scalar
          done
      - name: Interleave default and tagged benchmarks
        run: |
          set -euo pipefail
          output="${RUNNER_TEMP}/amd64-bench"
          for package in root field scalar; do
            : >"${output}/${package}-default.txt"
            : >"${output}/${package}-tagged.txt"
          done
          for kernel in none mul square; do
            : >"${output}/root-${kernel}.txt"
          done
          : >"${output}/root-w5.txt"
          for mode in accepted scalar all; do
            : >"${output}/root-v2-${mode}.txt"
          done
          for mode in accepted scalar-mul scalar-square scalar-squaren scalar; do
            : >"${output}/scalar-v2-${mode}.txt"
          done

          run_mode() {
            local mode="$1"
            local tags=()
            if [[ "${mode}" == tagged ]]; then
              tags=(-tags=secp256k1_asm)
            fi
            go test "${tags[@]}" -run '^$' \
              -bench '^Benchmark(SignDigest|SignRecoverableDigest|VerifyDigest|RecoverDigest|VerifyHotPublicKey|VerifyParseCompressedCold|VerifyParseUncompressedCold|SignCompact|SignRecoverable|PublicKeyDerive|ScalarInv|ScalarInvVartime|ScalarBaseMultProjective)$' \
              -benchmem -benchtime=500ms -count=1 . >>"${output}/root-${mode}.txt"
            go test "${tags[@]}" -run '^$' \
              -bench '^BenchmarkField(Mul|MulByB3|Square|SquareN)$' \
              -benchmem -benchtime=500ms -count=1 ./internal/field >>"${output}/field-${mode}.txt"
            go test "${tags[@]}" -run '^$' \
              -bench '^BenchmarkScalar(Mul|Square|SquareN|Inv)$' \
              -benchmem -benchtime=500ms -count=1 ./internal/scalar >>"${output}/scalar-${mode}.txt"
          }

          run_kernel() {
            local kernel="$1"
            SECP256K1_AMD64_BENCH_KERNEL="${kernel}" \
              go test -tags='secp256k1_asm,secp256k1_amd64_bench,secp256k1_amd64_w5_bench' -run '^$' \
                -bench '^Benchmark(SignRecoverable|VerifyHotPublicKey)$' \
                -benchmem -benchtime=500ms -count=1 . >>"${output}/root-${kernel}.txt"
          }

          run_w5() {
            go test -tags='secp256k1_asm,secp256k1_amd64_w5_bench' -run '^$' \
              -bench '^Benchmark(ScalarBaseMultProjective|SignRecoverable|VerifyHotPublicKey)$' \
              -benchmem -benchtime=500ms -count=1 . >>"${output}/root-w5.txt"
          }

          run_v2_root() {
            local label="$1"
            local selection="$2"
            SECP256K1_AMD64_V2_BENCH_KERNEL="${selection}" \
              go test -tags='secp256k1_asm,secp256k1_amd64_bench' -run '^$' \
                -bench '^Benchmark(SignDigest|SignRecoverableDigest|VerifyDigest|RecoverDigest|VerifyHotPublicKey|VerifyParseCompressedCold|VerifyParseUncompressedCold|SignCompact|SignRecoverable|PublicKeyDerive|ScalarInvVartime|ScalarBaseMultProjective)$' \
                -benchmem -benchtime=500ms -count=1 . >>"${output}/root-v2-${label}.txt"
          }

          run_v2_scalar() {
            local mode="$1"
            SECP256K1_AMD64_V2_BENCH_KERNEL="${mode}" \
              go test -tags='secp256k1_asm,secp256k1_amd64_bench' -run '^$' \
                -bench '^BenchmarkScalar(Mul|Square|SquareN|Inv)$' \
                -benchmem -benchtime=500ms -count=1 ./internal/scalar >>"${output}/scalar-v2-${mode}.txt"
          }

          for iteration in $(seq 1 10); do
            if (( iteration % 2 == 1 )); then
              run_mode default
              run_w5
              run_mode tagged
            else
              run_mode tagged
              run_w5
              run_mode default
            fi
            v2_labels=(accepted scalar all)
            v2_selections=(accepted scalar 'scalar,invvartime')
            for offset in 0 1 2; do
              index=$(( (iteration + offset - 1) % 3 ))
              run_v2_root "${v2_labels[index]}" "${v2_selections[index]}"
            done
            scalar_modes=(accepted scalar-mul scalar-square scalar-squaren scalar)
            for offset in 0 1 2 3 4; do
              index=$(( (iteration + offset - 1) % 5 ))
              run_v2_scalar "${scalar_modes[index]}"
            done
            kernels=(none mul square)
            for offset in 0 1 2; do
              index=$(( (iteration + offset - 1) % 3 ))
              run_kernel "${kernels[index]}"
            done
          done
      - name: Capture accepted and v2 CPU profiles
        run: |
          set -euo pipefail
          output="${RUNNER_TEMP}/amd64-bench"
          capture_profile() {
            local label="$1"
            local selection="$2"
            local benchmark="$3"
            SECP256K1_AMD64_V2_BENCH_KERNEL="${selection}" \
              go test -tags='secp256k1_asm,secp256k1_amd64_bench' -run '^$' \
                -bench "^Benchmark${benchmark}$" -benchtime=3s -count=1 \
                -cpuprofile="${output}/${label}-${benchmark}.cpu.pprof" . \
                >"${output}/${label}-${benchmark}.profile.txt"
          }
          capture_profile accepted accepted SignRecoverable
          capture_profile accepted accepted VerifyHotPublicKey
          capture_profile v2 'scalar,invvartime' SignRecoverable
          capture_profile v2 'scalar,invvartime' VerifyHotPublicKey
      - name: Build median reports
        run: |
          set -euo pipefail
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp "${output}/root-default.txt" "${output}/root-tagged.txt" \
            2>&1 | tee "${output}/root-summary.txt"
          go run ./cmd/benchcmp "${output}/field-default.txt" "${output}/field-tagged.txt" \
            2>&1 | tee "${output}/field-summary.txt"
          go run ./cmd/benchcmp "${output}/scalar-default.txt" "${output}/scalar-tagged.txt" \
            2>&1 | tee "${output}/scalar-summary.txt"
          go run ./cmd/benchcmp "${output}/root-w5.txt" "${output}/root-tagged.txt" \
            2>&1 | tee "${output}/root-w6-stage-summary.txt"
          for kernel in mul square; do
            go run ./cmd/benchcmp "${output}/root-none.txt" "${output}/root-${kernel}.txt" \
              2>&1 | tee "${output}/root-${kernel}-summary.txt"
          done
          go run ./cmd/benchcmp "${output}/root-v2-accepted.txt" "${output}/root-v2-scalar.txt" \
            2>&1 | tee "${output}/root-v2-scalar-summary.txt"
          go run ./cmd/benchcmp "${output}/root-v2-scalar.txt" "${output}/root-v2-all.txt" \
            2>&1 | tee "${output}/root-v2-invvartime-summary.txt"
          go run ./cmd/benchcmp "${output}/root-v2-accepted.txt" "${output}/root-v2-all.txt" \
            2>&1 | tee "${output}/root-v2-final-summary.txt"
          go run ./cmd/benchcmp "${output}/scalar-v2-accepted.txt" "${output}/scalar-v2-scalar.txt" \
            2>&1 | tee "${output}/scalar-v2-summary.txt"
          for kernel in scalar-mul scalar-square scalar-squaren; do
            go run ./cmd/benchcmp "${output}/scalar-v2-accepted.txt" "${output}/scalar-v2-${kernel}.txt" \
              2>&1 | tee "${output}/scalar-v2-${kernel}-summary.txt"
          done
      - name: Check per-kernel contribution thresholds
        run: |
          set -euo pipefail
          output="${RUNNER_TEMP}/amd64-bench"
          for kernel in mul square; do
            go run ./cmd/benchcmp -gate=kernel "${output}/root-none.txt" "${output}/root-${kernel}.txt"
          done
      - name: Check base-field thresholds
        run: |
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp -gate=field "${output}/field-default.txt" "${output}/field-tagged.txt"
      - name: Check W6 incremental thresholds
        run: |
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp -gate=w6 "${output}/root-w5.txt" "${output}/root-tagged.txt"
      - name: Check v2 scalar thresholds
        run: |
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp -gate=v2-scalar-micro "${output}/scalar-v2-accepted.txt" "${output}/scalar-v2-scalar.txt"
          go run ./cmd/benchcmp -gate=v2-scalar-e2e "${output}/root-v2-accepted.txt" "${output}/root-v2-scalar.txt"
      - name: Check v2 public inversion thresholds
        if: always()
        run: |
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp -gate=v2-invvartime "${output}/root-v2-scalar.txt" "${output}/root-v2-all.txt"
      - name: Check v2 final incremental thresholds
        if: always()
        run: |
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp -gate=v2-final "${output}/root-v2-accepted.txt" "${output}/root-v2-all.txt"
      - name: Check final end-to-end thresholds
        if: always()
        run: |
          output="${RUNNER_TEMP}/amd64-bench"
          go run ./cmd/benchcmp -gate=final "${output}/root-default.txt" "${output}/root-tagged.txt"
      - name: Upload raw benchmark data
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: amd64-${{ matrix.goamd64 }}-benchmarks
          path: ${{ runner.temp }}/amd64-bench
